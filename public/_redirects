# Netlify redirects for Single Page Application
# This file tells Netlify to serve index.html for all routes
# so React Router can handle client-side routing

# Main SPA redirect - serve index.html for all routes
/*    /index.html   200

# Optional: Specific redirects for common paths
/admin    /index.html   200
/booking  /index.html   200
/events   /index.html   200
/services /index.html   200
/contact  /index.html   200